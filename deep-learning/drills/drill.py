import numpy as np

# question 1
arr = np.zeros((7, 7)).astype(int)
arr += np.arange(7)
print("Question 1:\n")
print(arr)

# question 2


# question 3
"""
stochastic gradient descent saves time since it only uses
one or a subset of the data points in order to
approximate the gradient instead of needing to find the
sum over all data points like standard gradient descent does
"""
